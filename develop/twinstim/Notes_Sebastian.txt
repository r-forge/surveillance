--------------------------------------------------------------------------------
| SEBASTIAN'S PERSONAL TODO LIST                                               |
| This file contains notes of every description.                               |
| $Date$                        |
--------------------------------------------------------------------------------

- plot.epidataCS (type=c("temporal", "spatial"))

- Bei NA-Zuordnung von tile eine warning schmeißen statt abzubrechen (abbruch erfolgt zur zeit mit na.fail in buildmmh)
- REPORT argument in simEpidataCS
- kreise "simplifizieren". geht mit simplify.owin(as.owin(as(kreise, "gpc.poly")), 10) oder http://mapshaper.com/test/demo.html
- twinstim-parametrisierung ändern. es soll möglich sein ohne optimierung ein twinstim objekt zu erhalten, dass auf den startwerten basiert. nicht optim.args=NULL sondern neuer parameter "do.optim" oder so ähnlich (vgl. andere pakete)
- update-funktion für "epidataCS" bzgl. stgrid variablen hinzufügen oder löschen (NULL). was ist mit zusätzlichen markierungen? => "marks<-"
- möglichkeit für drop.unused.levels = FALSE in twinstim und as.epidataCS. das geht wohl nur ohne optimierung?

- funktion um nachträglich tau zu berechnen, wenn model=TRUE.
- plot.twinstim (= residual plot or serial correlation plot). call function for a posteriori calculation of tau if tau is not part of the twinstim-object
- integrate tiebreak functions into RLadyBug

- environments optimieren, damit fits beim speichern nicht soviel speicherplatz beanspruchen.
- R0-Bug bei Faktorvariablen
- optimierung erst adaptiv, dann fest wegen konvergenz! ODER SCHAUKELALGORITHMUS!

- Rd-doku!!

- any simplifications in simulation from twinstim objects?

- es wäre natürlich trotzdem mal interessant effRangeMult auf Inf zu setzen und die Ergebnisse zu vergleichen.
- test wie es mit echter gaußdichte funktioniert (nicht wert 1 bei 0)
- test mit lower für sigma
- validierung von twinstim durch twinSIR bei nur endemischer komponente

- irgendwie typ-spezifische endemische kovariablen erlauben (influenza!). über "special" kriegt man den intercept nicht typspezifisch. also: eigenes argument typeSpecificEffects = c("(Intercept)", "influenza0")... vorsicht mit kategorialen variablen => ergeben mehrere parameter



# If we include twinstim in RLadyBug we should add a licence note like e.g. in maptools
  Note: polygon geometry computations in maptools
        depend on the package gpclib, which has a
        restricted licence. It is disabled by default;
        to enable gpclib, type gpclibPermit()


### DESCRIPTION file extras for twinstim
Depends:        R (>= 2.10.1), sp, gpclib, spatstat (>= 1.17-0), maptools
Imports:        intervals
Suggests: statmod, xtable, animation, RColorBrewer
        3. Continuous spatio-temporal point processes are modeled through
        additive-multiplicative conditional intensities (fitted by the
        'twinstim' function).


Online sources on R packages for spatio(-temporal) analysis
-----------------------------------------------------------
http://cran.at.r-project.org/web/views/Spatial.html
http://r-spatial.sourceforge.net/
https://stat.ethz.ch/mailman/listinfo/r-sig-geo
http://geodacenter.asu.edu/r-spatial-projects/ (A number of notes about spatial analysis appeared in R News in 2001-2002)
http://www.spatstat.org/spatstat/spatstat2.html
http://www.geog.uu.nl/~pebesma/wun/
http://homepages.paradise.net.nz/david.harte/SSLib/


R packages
----------
geostatistical data: gstat, geoR/geoRglm, fields, RandomFields, spBayes
point patterns:
    spatial (Ripley: "Functions for kriging and point pattern analysis")
    splancs (Rowlingson, Diggle, Bivand: "Spatial and Space-Time Point Pattern Analysis")
    spatstat (Baddeley, Turner: "Spatial Point Pattern analysis, model-fitting, simulation, tests")
    [spatgraphs (neighbourhood graphs for spatial point processes), MarkedPointProcess (Analysis of Marks), spatialkernel (Nonparameteric (edge-corrected kernel density) estimation of spatial segregation), spatclus (Spatial Cluster Detection)] <- bad R style
    PtProcess, SSLib (fitting/modelling the conditional intensity function for events ordered by time !!!)
    spsurvey (Olson, Stevens: "Spatial Survey Design and Analysis", spatial sampling)
lattice/area data: spdep, DCluster (detection of spatial clusters of diseases), spgwr
